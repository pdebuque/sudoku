import React from 'react'

import { DateTime, Duration } from 'luxon'

interface Props {
  start: DateTime;
  time: DateTime;

}

const Timer = () => {
  return (
    <div>Timer</div>
  )
}

export default Timer